import { atom } from "jotai";

const currentPagePopularAtom = atom(1);
const currentPageUpcomingAtom = atom(1);
const currentPageTopAtom = atom(1);
export { currentPagePopularAtom, currentPageUpcomingAtom, currentPageTopAtom };
